import 'package:flutter/material.dart';

// App color scheme
class AppColors {
  // Primary background color for dark mode interface
  static const charcoalBlack = Color(0xFF121212);
  
  // For card backgrounds and secondary elements
  static const deepSlateGray = Color(0xFF1E1E1E);
  
  // A calming accent color for buttons and highlights
  static const mutedTeal = Color(0xFF007B7F);
  
  // For notifications and status indicators to add vibrancy
  static const warmCoral = Color(0xFFFF5B5B);
  
  // For text and icons to ensure readability
  static const coolGray = Color.fromARGB(255, 176, 190, 197);
  
  // For highlights and call-to-action elements
  static const softLemonYellow = Color(0xFFFFD54F);
}
